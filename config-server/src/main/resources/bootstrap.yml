spring:
  application:
    name:
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/vip96300/micro-company
          label: master
          search-paths: configfile
          username: vip96300@163.com
          password:

security:
  user:
    password: password
    
server:
  port: ${PORT:8888} 